// Configuração LiveKit para a Arena.
// Para usar live real, substitui estes valores pelos teus endpoints.
// Exemplo:
// window.SHITAGRAM_LIVEKIT_URL = "wss://teu-projeto.livekit.cloud";
// window.SHITAGRAM_LIVEKIT_TOKEN_ENDPOINT = "https://teu-dominio.pt/api/livekit-token";

// Importante: a chave secreta do LiveKit nunca deve ficar neste ficheiro.
// O token deve ser gerado num backend seguro.

window.SHITAGRAM_LIVEKIT_URL = "";
window.SHITAGRAM_LIVEKIT_TOKEN_ENDPOINT = "";
